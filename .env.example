# 複製成 .env（已在 .gitignore，不會進版控），或直接貼進 Render 的
# Environment 分頁。兩個都不設就是純本機模式：state 存本機檔案、網頁無需
# 存取碼 —— 跟改動前完全一樣。

# Supabase session pooler 連線字串（Dashboard → Connect → Session pooler，
# 不要用 Transaction pooler：atrgrid serve 是長連線的 process，不是無狀態
# function）。設了這個，state 就改讀寫 Postgres，忽略 --state 指定的檔案路徑。
ATRGRID_DATABASE_URL=postgresql://postgres.xxxx:PASSWORD@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres

# 部署到 Render（或任何非 127.0.0.1 的地方）時務必設 —— 前端每次 API 呼叫
# 都要帶這個字串當 X-Auth-Token header，否則回 401。自己找一串夠長的隨機字串。
ATRGRID_AUTH_TOKEN=change-me-to-a-long-random-string
